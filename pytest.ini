[pytest]
markers =
    parking: Тесты операций парковки (заезд/выезд)

testpaths = tests
addopts = -v --tb=short --strict-markers

python_files = test_*.py
python_classes = Test*
python_functions = test_*
